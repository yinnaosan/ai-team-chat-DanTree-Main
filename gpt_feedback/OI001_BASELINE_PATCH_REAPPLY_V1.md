# OI-001 BASELINE_PATCH_REAPPLY_V1 — Validation Report

**Date:** 2026-04-12  
**Checkpoint:** `79938089`  
**Task:** 补回 OI-001 旧 patch，恢复干净基线

---

## 1. 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/model_router.test.ts` | 修改 | TC-MR-01 × 2 + TC-MR-04 × 4，共 6 处 |
| `server/llmProviders.ts` | 修改 | gpt-5.4 `recommended_for` 追加 `"agentic_tasks"` |

**未修改（严格遵守禁止范围）：**
- `server/model_router.ts` ✅
- `server/_core/llm.ts` ✅
- `server/routers.ts` ✅
- `server/taskTypeBridge.ts` ✅
- `server/taskTypeBridge.test.ts` ✅
- 所有数据层 / 主链文件 ✅

---

## 2. 7 个 patch 的实际落仓说明

### TC-MR-01（2 个）— 非法 / 空 task_type fallback 行为

**旧断言（失败）：**
```ts
await expect(modelRouter.generate(..., "invalid_type")).rejects.toThrow(/Invalid task_type/);
await expect(modelRouter.generate(..., "")).rejects.toThrow(/Invalid task_type/);
```

**新断言（通过）：**
```ts
const result = await modelRouter.generate({ messages: SAMPLE_MESSAGES }, "invalid_type" as TaskType);
expect(result).toBeDefined();
expect(typeof result.output).toBe("string");
expect(result.metadata?.task_type).toBe("default");
expect(["anthropic", "openai", "gpt_stub"]).toContain(result.provider);
```

**设计说明：**
- `generate()` 当前实现对非法 task_type 走 fallback to "default"，不 throw
- provider 不固定为 `"gpt_stub"`，因为研发态下 Claude（anthropic）仍可能是实际执行路径
- 合法集合断言 `["anthropic", "openai", "gpt_stub"]` 覆盖所有可能的研发 / 生产路径

### TC-MR-04（4 个）— 生产态 routing 期望对齐

| Test | 旧期望 | 新期望 | 原因 |
|------|--------|--------|------|
| research → ? | `anthropic` | `openai` | OI-001 GPT 主控策略：research 归 GPT |
| narrative → ? | `anthropic` | `openai` | OI-001 GPT 主控策略：narrative 归 GPT |
| summarization → ? | `anthropic` | `openai` | OI-001 GPT 主控策略：summarization 归 GPT |
| routingFor("research") → ? | `anthropic` + `claude-*` | `openai` + `model.length > 0` | 同上，放宽 model 断言 |

**设计说明：**
- `PRODUCTION_ROUTING_MAP` 中 research / narrative / summarization 实际值均为 `"openai"`
- `routingFor("research", "production")` 返回 `{ provider: "openai", model: "gpt-5.4" }`
- 旧测试期望 `anthropic` 是历史遗留，与当前实现不符

### TC-LLM-04（1 个）— recommendModel("agentic_tasks") → gpt-5.4

**修改位置：** `server/llmProviders.ts` L173

```ts
// 旧
recommended_for: ["deep_research", "complex_reasoning", "professional_workflows", "structured_json"],

// 新
recommended_for: ["agentic_tasks", "deep_research", "complex_reasoning", "professional_workflows", "structured_json"],
```

**设计说明：**
- `recommendModel()` 逻辑：`Object.values(MODEL_METADATA).find(m => m.recommended_for.includes(useCase))`
- `claude-opus-4-6` 的 `recommended_for` 包含 `"agent_tasks"`（不同拼写），不包含 `"agentic_tasks"`
- 追加 `"agentic_tasks"` 到 `gpt-5.4` 后，`recommendModel("agentic_tasks")` 返回 `"gpt-5.4"` ✅
- `"agentic_tasks"` 放在数组首位，确保 find() 优先命中

---

## 3. 测试结果

### TSC
```
npx tsc --noEmit
→ 0 errors  ✅
```

### model_router.test.ts + llmProviders.test.ts
```
npx vitest run server/model_router.test.ts server/llmProviders.test.ts

 Test Files  2 passed (2)
      Tests  47 passed (47)
   Duration  2.93s
```

**全部 47/47 通过 ✅**（原 7 failures 全部消除）

### taskTypeBridge.test.ts（Bridge v1 回归）
```
npx vitest run server/taskTypeBridge.test.ts

 Test Files  1 passed (1)
      Tests  22 passed (22)
   Duration  359ms
```

**全部 22/22 通过 ✅**（TaskType Bridge v1 完整保留，不受影响）

---

## 4. 新增 failures

**零新增 failure ✅**

---

## 5. Checkpoint

**`79938089`** — OI-001 BASELINE_PATCH_REAPPLY_V1 落仓完成

---

## 6. 完成标准核验

| 条件 | 状态 |
|------|------|
| OI-001 旧 7 个 failures 全部消除 | ✅ |
| TaskType Bridge v1 不被打坏（22/22） | ✅ |
| 无新增 failures | ✅ |
| 未越界修改禁止文件 | ✅ |

---

## 7. 遗留事项（供 GPT 下轮决策）

### 7.1 TC-MR-01 fallback observability
当前 TC-MR-01 只断言 `result.metadata?.task_type === "default"`，不断言 `fallback_applied`。

原因：`generate()` 的 `metadata` 对象当前不包含 `fallback_applied` 字段（该字段在 log 对象中，未写入 RouterResponse）。

**决策点：** 是否在 `model_router.ts` 中把 `fallback_applied: true` 写入 `RouterResponse.metadata`？
- 若是：TC-MR-01 可补充更精确的断言，observability 增强
- 若否：当前断言已足够，保持现状

### 7.2 `agent_tasks` vs `agentic_tasks` 命名不一致
- `claude-opus-4-6.recommended_for` 包含 `"agent_tasks"`
- `gpt-5.4.recommended_for` 现在包含 `"agentic_tasks"`
- 两者是不同字符串，`recommendModel("agent_tasks")` 返回 `claude-opus-4-6`，`recommendModel("agentic_tasks")` 返回 `gpt-5.4`

**决策点：** 是否需要统一命名规范？
- 选项 A：保持现状（两个不同 use case，语义上可区分）
- 选项 B：统一为 `"agentic_tasks"`（claude-opus-4-6 也追加，或改名）
- 选项 C：统一为 `"agent_tasks"`（gpt-5.4 改名）

### 7.3 剩余 14 个 invokeLLM 调用点 triggerContext 补全
TaskType Bridge v1 只覆盖了 4 个最高语义价值的调用点（step3_main / memory_summary / repair_pass / title_gen）。其余 14 个调用点仍走 `"default"`。

**决策点：** 是否在下一轮系统性补全？需要 GPT 提供 patch delivery 或授权 Manus 自行判断。

---

*Report generated by Manus (Core Engineer) — OI-001 Baseline Patch Reapply v1*
